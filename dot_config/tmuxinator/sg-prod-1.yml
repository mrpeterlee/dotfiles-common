name: sg-prod-1
root: ~/acap
startup_pane: 1

windows:
  - ACap-1:
      layout: even-horizontal
      panes:
        - acap-1:
            - cd ~/acap
            - curl "wttr.in/singapore?m&lang=zh-cn"

  - ACap-2:
      layout: even-horizontal
      panes:
        - acap-2:
            - cd ~/acap

  - ACap-3:
      layout: even-horizontal
      panes:
        - acap-3:
            - cd ~/acap

  - ACap-4:
      layout: even-horizontal
      panes:
        - acap-4:
            - cd ~/acap

  - ACap-5:
      layout: even-horizontal
      panes:
        - acap-5:
            - cd ~/acap

  - sg-prod-1_CCs:
      layout: tiled
      panes:
        - acap_cc_1:
            - mkdir -p /tmp/claude_telegram_sessions/acap_cc_1
            - ensure_file() { local dst="$1" src="$2"; if [[ ! -f "$dst" ]]; then cp "$src" "$dst"; fi }
            - ensure_file /tmp/claude_telegram_sessions/acap_cc_1/access.json ~/.claude/channels/telegram/access.json
            - export TELEGRAM_BOT_TOKEN="$(op read 'op://ACap/Telegram Bot - acap_cc1_bot/credential')"
            - export TELEGRAM_STATE_DIR="/tmp/claude_telegram_sessions/acap_cc_1"
            - sleep 1 && export BW_SESSION=$(bw unlock "$(op read 'op://Tapai/Bitwarden/password')" --raw)
            - cd ~/acap
            - claude --worktree acap_cc_1 --channels plugin:telegram@claude-plugins-official --dangerously-skip-permissions

        - acap_cc_2:
            - sleep 28
            - mkdir -p /tmp/claude_telegram_sessions/acap_cc_2
            - ensure_file() { local dst="$1" src="$2"; if [[ ! -f "$dst" ]]; then cp "$src" "$dst"; fi }
            - ensure_file /tmp/claude_telegram_sessions/acap_cc_2/access.json ~/.claude/channels/telegram/access.json
            - export TELEGRAM_BOT_TOKEN="$(op read 'op://ACap/Telegram Bot - acap_cc2_bot/credential')"
            - export TELEGRAM_STATE_DIR="/tmp/claude_telegram_sessions/acap_cc_2"
            - sleep 1 && export BW_SESSION=$(bw unlock "$(op read 'op://Tapai/Bitwarden/password')" --raw)
            - cd ~/acap
            - claude --worktree acap_cc_2 --channels plugin:telegram@claude-plugins-official --dangerously-skip-permissions

        - alpha_cc_1:
            - sleep 58
            - mkdir -p /tmp/claude_telegram_sessions/acap_cc_5
            - ensure_file() { local dst="$1" src="$2"; if [[ ! -f "$dst" ]]; then cp "$src" "$dst"; fi }
            - ensure_file /tmp/claude_telegram_sessions/acap_cc_5/access.json ~/.claude/channels/telegram/access.json
            - export TELEGRAM_BOT_TOKEN="$(op read 'op://ACap/Telegram Bot - acap_cc5_bot/credential')"
            - export TELEGRAM_STATE_DIR="/tmp/claude_telegram_sessions/acap_cc_5"
            - sleep 1 && export BW_SESSION=$(bw unlock "$(op read 'op://Tapai/Bitwarden/password')" --raw)
            - cd ~/acap
            - claude --worktree alpha_cc_1 --channels plugin:telegram@claude-plugins-official --dangerously-skip-permissions

        - alpha_cc_2:
            - sleep 113
            - mkdir -p /tmp/claude_telegram_sessions/acap_cc_6
            - ensure_file() { local dst="$1" src="$2"; if [[ ! -f "$dst" ]]; then cp "$src" "$dst"; fi }
            - ensure_file /tmp/claude_telegram_sessions/acap_cc_6/access.json ~/.claude/channels/telegram/access.json
            - export TELEGRAM_BOT_TOKEN="$(op read 'op://ACap/Telegram Bot - acap_cc6_bot/credential')"
            - export TELEGRAM_STATE_DIR="/tmp/claude_telegram_sessions/acap_cc_6"
            - sleep 1 && export BW_SESSION=$(bw unlock "$(op read 'op://Tapai/Bitwarden/password')" --raw)
            - cd ~/acap
            - claude --worktree alpha_cc_2 --channels plugin:telegram@claude-plugins-official --dangerously-skip-permissions


  - acap-admin_CCs:
      layout: tiled
      panes:
        - acap_cc_3:
            - autossh -M 0 -o "ServerAliveInterval=30" -o "ServerAliveCountMax=3" -t acap "tmux new -A -s cc_acap_cc_3"
            - mkdir -p /tmp/claude_telegram_sessions/acap_cc_3
            - ensure_file() { local dst="$1" src="$2"; if [[ ! -f "$dst" ]]; then cp "$src" "$dst"; fi }
            - ensure_file /tmp/claude_telegram_sessions/acap_cc_3/access.json ~/.claude/channels/telegram/access.json
            - export TELEGRAM_BOT_TOKEN="$(op read 'op://ACap/Telegram Bot - acap_cc3_bot/credential')"
            - export TELEGRAM_STATE_DIR="/tmp/claude_telegram_sessions/acap_cc_3"
            - sleep 1 && export BW_SESSION=$(bw unlock "$(op read 'op://Tapai/Bitwarden/password')" --raw)
            - cd ~/acap
            - claude --worktree acap_cc_3 --channels plugin:telegram@claude-plugins-official --dangerously-skip-permissions

        - acap_cc_4:
            - autossh -M 0 -o "ServerAliveInterval=30" -o "ServerAliveCountMax=3" -t acap "tmux new -A -s cc_acap_cc_4"
            - sleep 17
            - mkdir -p /tmp/claude_telegram_sessions/acap_cc_4
            - ensure_file() { local dst="$1" src="$2"; if [[ ! -f "$dst" ]]; then cp "$src" "$dst"; fi }
            - ensure_file /tmp/claude_telegram_sessions/acap_cc_4/access.json ~/.claude/channels/telegram/access.json
            - export TELEGRAM_BOT_TOKEN="$(op read 'op://ACap/Telegram Bot - acap_cc4_bot/credential')"
            - export TELEGRAM_STATE_DIR="/tmp/claude_telegram_sessions/acap_cc_4"
            - sleep 1 && export BW_SESSION=$(bw unlock "$(op read 'op://Tapai/Bitwarden/password')" --raw)
            - cd ~/acap
            - claude --worktree acap_cc_4 --channels plugin:telegram@claude-plugins-official --dangerously-skip-permissions

        - alpha_cc_3:
            - autossh -M 0 -o "ServerAliveInterval=30" -o "ServerAliveCountMax=3" -t acap "tmux new -A -s cc_alpha_cc_3"
            - sleep 170
            - mkdir -p /tmp/claude_telegram_sessions/acap_cc_7
            - ensure_file() { local dst="$1" src="$2"; if [[ ! -f "$dst" ]]; then cp "$src" "$dst"; fi }
            - ensure_file /tmp/claude_telegram_sessions/acap_cc_7/access.json ~/.claude/channels/telegram/access.json
            - export TELEGRAM_BOT_TOKEN="$(op read 'op://ACap/Telegram Bot - acap_cc7_bot/credential')"
            - export TELEGRAM_STATE_DIR="/tmp/claude_telegram_sessions/acap_cc_7"
            - sleep 1 && export BW_SESSION=$(bw unlock "$(op read 'op://Tapai/Bitwarden/password')" --raw)
            - cd ~/acap
            - claude --worktree alpha_cc_3 --channels plugin:telegram@claude-plugins-official --dangerously-skip-permissions


  - Tapai-CCs:
      layout: tiled
      panes:
        - tapai_cc_1:
            - autossh -M 0 -o "ServerAliveInterval=30" -o "ServerAliveCountMax=3" -t tapai "tmux new -A -s cc_tapai_cc_1"
            - mkdir -p /tmp/claude_telegram_sessions/tapai_cc_1
            - ensure_file() { local dst="$1" src="$2"; if [[ ! -f "$dst" ]]; then cp "$src" "$dst"; fi }
            - ensure_file /tmp/claude_telegram_sessions/tapai_cc_1/access.json ~/.claude/channels/telegram/access.json
            - export TELEGRAM_BOT_TOKEN="$(op read 'op://Tapai/Telegram Bot - tapai_cc1_bot/credential')"
            - export TELEGRAM_STATE_DIR="/tmp/claude_telegram_sessions/tapai_cc_1"
            - sleep 1 && export BW_SESSION=$(bw unlock "$(op read 'op://Tapai/Bitwarden/password')" --raw)
            - cd ~/tapai
            - claude --worktree tapai_cc_1 --channels plugin:telegram@claude-plugins-official --dangerously-skip-permissions

        - tapai_cc_2:
            - autossh -M 0 -o "ServerAliveInterval=30" -o "ServerAliveCountMax=3" -t tapai "tmux new -A -s cc_tapai_cc_2"
            - sleep 17
            - mkdir -p /tmp/claude_telegram_sessions/tapai_cc_2
            - ensure_file() { local dst="$1" src="$2"; if [[ ! -f "$dst" ]]; then cp "$src" "$dst"; fi }
            - ensure_file /tmp/claude_telegram_sessions/tapai_cc_2/access.json ~/.claude/channels/telegram/access.json
            - export TELEGRAM_BOT_TOKEN="$(op read 'op://Tapai/Telegram Bot - tapai_cc2_bot/credential')"
            - export TELEGRAM_STATE_DIR="/tmp/claude_telegram_sessions/tapai_cc_2"
            - sleep 1 && export BW_SESSION=$(bw unlock "$(op read 'op://Tapai/Bitwarden/password')" --raw)
            - cd ~/tapai
            - claude --worktree tapai_cc_2 --channels plugin:telegram@claude-plugins-official --dangerously-skip-permissions

        - tapai_cc_3:
            - autossh -M 0 -o "ServerAliveInterval=30" -o "ServerAliveCountMax=3" -t tapai "tmux new -A -s cc_tapai_cc_3"
            - sleep 39
            - mkdir -p /tmp/claude_telegram_sessions/tapai_cc_3
            - ensure_file() { local dst="$1" src="$2"; if [[ ! -f "$dst" ]]; then cp "$src" "$dst"; fi }
            - ensure_file /tmp/claude_telegram_sessions/tapai_cc_3/access.json ~/.claude/channels/telegram/access.json
            - export TELEGRAM_BOT_TOKEN="$(op read 'op://Tapai/Telegram Bot - tapai_cc3_bot/credential')"
            - export TELEGRAM_STATE_DIR="/tmp/claude_telegram_sessions/tapai_cc_3"
            - sleep 1 && export BW_SESSION=$(bw unlock "$(op read 'op://Tapai/Bitwarden/password')" --raw)
            - cd ~/tapai
            - claude --worktree tapai_cc_3 --channels plugin:telegram@claude-plugins-official --dangerously-skip-permissions

        - tapai_cc_4:
            - autossh -M 0 -o "ServerAliveInterval=30" -o "ServerAliveCountMax=3" -t tapai "tmux new -A -s cc_tapai_cc_4"
            - sleep 61
            - mkdir -p /tmp/claude_telegram_sessions/tapai_cc_4
            - ensure_file() { local dst="$1" src="$2"; if [[ ! -f "$dst" ]]; then cp "$src" "$dst"; fi }
            - ensure_file /tmp/claude_telegram_sessions/tapai_cc_4/access.json ~/.claude/channels/telegram/access.json
            - export TELEGRAM_BOT_TOKEN="$(op read 'op://Tapai/Telegram Bot - tapai_cc4_bot/credential')"
            - export TELEGRAM_STATE_DIR="/tmp/claude_telegram_sessions/tapai_cc_4"
            - sleep 1 && export BW_SESSION=$(bw unlock "$(op read 'op://Tapai/Bitwarden/password')" --raw)
            - cd ~/tapai
            - claude --worktree tapai_cc_4 --channels plugin:telegram@claude-plugins-official --dangerously-skip-permissions

  - monitor:
      layout: tiled
      panes:
        - glances:
            - cd
            # Build latest lean
            ## - bash /lab/paper/ts/lean/bin/Build-Custom-Lean
            ## - bash /lab/paper/ts/lean/bin/Build-Custom-Lean --tester

            # Build datalab
            ## - . parse_yml ${HOME}/.lab/github.yml && cd /lab/paper/datalab_core && make build
            ## - bash -c ". parse_yml ${HOME}/.lab/github.yml && cd /lab/paper/datalab_core && make build && make push"
            ## - bash -c ". parse_yml ${HOME}/.lab/github.yml && cd /lab/prod/datalab_core && git checkout prod && git fetch --all && git reset --hard origin/main && make build labenv=prod dbenv=prod && make push labenv=prod dbenv=prod"

          #- glances
          # - ts flightlog stream -d

          # - docker-monitor:
          #     layout: tiled
          #     panes:
          #       - lazydocker:
          #         - docker run --rm -it -v /var/run/docker.sock:/var/run/docker.sock -v /home/peter/.config/lazydocker:/.config/jesseduffield/lazydocker lazyteam/lazydocker
          #       - cpu_temperature:
          #         - watch -n 10 -d sensors

  - background:
      layout: tiled
      panes:
        - backgrounds-tasks1:
            - cd
            ## - cd /lab/paper/finclab && git pull
            ## - cd /lab/paper/datalab && git pull
            ## - cd /lab/paper/ts && git pull
            ## - cd /lab/paper/winclient && git pull
            ## - cd /lab/paper/tradestation && git pull
            ## - cd /lab/paper/lab && git pull
            # Update xtquant lib
            ## - rm -rf /lab/lib/xtquant && cp /lab/data/app/xtquant.zip /lab/lib/xtquant.zip && cd /lab/lib && unzip -a xtquant.zip && rm -f xtquant.zip
            #    - jupyter notebook --port=$JUPYTER_PORT --no-browser --NotebookApp.token="" --notebook-dir=$HOME
        # - skype-bot:
        # - python /lab/paper/finclab/finclab/sh/bin/skype.py
        - backgrounds-tasks2:
            - cd

        #- make -C /lab/prod/labenv start
# Optional tmux socket
# socket_name: foo

# Note that the pre and post options have been deprecated and will be replaced by
# project hooks.

# Project hooks
# Runs on project start, always
# on_project_start: command
# Run on project start, the first time
# on_project_first_start: command
# Run on project start, after the first time
# on_project_restart: command
# Run on project exit ( detaching from tmux session )
# on_project_exit: command
# Run on project stop
# on_project_stop: command

# Runs in each window and pane before window/pane specific commands. Useful for setting up interpreter versions.
# pre_window: rbenv shell 2.0.0-p247

# Pass command line options to tmux. Useful for specifying a different tmux.conf.
# tmux_options: -f ~/.tmux.mac.conf

# Change the command to call tmux.  This can be used by derivatives/wrappers like byobu.
# tmux_command: byobu

# Specifies (by name or index) which window will be selected on project startup. If not set, the first window is used.
# startup_window: editor

# Specifies (by index) which pane of the specified window will be selected on project startup. If not set, the first pane is used.
# startup_pane: 1

# Controls whether the tmux session should be attached to automatically. Defaults to true.
# attach: false
