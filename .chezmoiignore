# README and documentation
README.md
/AGENTS.md
/CLAUDE.md
LICENSE

# CLI scripts (invoked from repo, not deployed to ~)
cli.bat
cli.ps1

# Git-specific (chezmoi handles .git automatically)
.gitignore
.gitattributes

# Platform-specific exclusions
{{ if not (eq .chezmoi.os "windows") }}
# Ignore Windows configs on non-Windows
.config_windows/**
.config/powershell/**
.chezmoiscripts/*.ps1
{{ end }}

{{ if not (eq .chezmoi.os "darwin") }}
# Ignore macOS-specific on non-Darwin
.DS_Store
{{ end }}

{{ if eq .chezmoi.os "windows" }}
# On Windows, ignore all Unix-specific files
.bashrc
.bash_profile
.bash_alias
.condarc
.config/zsh/**
.config/tmux/**
.config/tmuxinator/**
.config/nvim/**
.chezmoiscripts/*.sh
cli
env/**
{{ end }}

# Windows reserved filenames
nul

# Temporary and cache files
*.zwc
.zcompdump*
*.pyc
__pycache__/
.cache/
*.log

# Editor artifacts
*.swp
*.swo
*~

# === P2.5: Claude/Codex/Gemini ephemeral state (from .agents .chezmoiignore) ===

# Claude Code OAuth + secrets
.claude/.credentials.json
.claude/channels/telegram/.env
.claude/settings.local.json
.claude/README.md
.claude/PLUGIN_SCHEMA_NOTES.md
.claude/the-security-guide.md

# Claude Code session/runtime state
.claude/sessions/
.claude/session-env/
.claude/session-data/
.claude/projects/
.claude/debug/
.claude/cache/
.claude/backups/
.claude/file-history/
.claude/homunculus/
.claude/shell-snapshots/
.claude/paste-cache/
.claude/metrics/
.claude/telemetry/
.claude/chrome/
.claude/downloads/
.claude/todos/
.claude/memory/
.claude/ecc/
.claude/history.jsonl
.claude/bash-commands.log
.claude/cost-tracker.log
.claude/mcp-health-cache.json
.claude/stats-cache.json
.claude/policy-limits.json
.claude/private_dot_claude.json
.claude/private_dot_claude.json.stale.bak
.claude/security_warnings_state_*.json

# Plugin-managed (lives under .claude but not source)
.claude/plugins/
.claude/hooks/
.claude/skills/gstack/

# Nested agent configs (when run from a Claude Code session)
.claude/.agents/
.claude/.codex/
.claude/.cursor/
.claude/.opencode/

# Codex CLI ephemeral state
.codex/sessions/
.codex/cache/
.codex/log/
.codex/state_*.sqlite*
.codex/protocol/
.codex/auth.json

# Gemini CLI ephemeral state
.gemini/sessions/
.gemini/cache/
.gemini/oauth_creds.json

# === P2.5 follow-up: team inboxes are session-state, not config ===
# Each inbox is a JSON message log between agents from a one-off
# bootstrap session — contains operator-personal data (SSH passwords,
# AWS account IDs, infra IPs, etc.) that the org-plaintext regex
# doesn't fully catch. Treat as session-state, like .claude/sessions/.
private_dot_claude/teams/*/inboxes/

# === P2.5 follow-up: channels/ runtime + access files ===
# bot.pid is process state; private_access.json holds operator allowlist
# (Telegram chat_ids etc.). Same structural intent as the existing
# session-state guards.
private_dot_claude/channels/*/bot.pid
private_dot_claude/channels/*/private_access.json
